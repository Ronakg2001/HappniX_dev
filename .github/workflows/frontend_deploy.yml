name: Frontend Deploy

on:
  push:
    branches:
      - main
    paths:
      - "web_frontend/**"
      - ".github/workflows/frontend_deploy.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Write runtime config
        shell: bash
        run: |
          cat <<EOF > web_frontend/runtime-config.js
          window.HAPPNIX_RUNTIME_CONFIG = {
            apiBaseUrl: "${{ secrets.API_BASE_URL }}"
          };
          EOF

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy web_frontend --project-name=${{ secrets.CLOUDFLARE_PAGES_PROJECT }}
