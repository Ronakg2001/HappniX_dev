// =========================================================================
// DEV TOOLS - BROWSER CONSOLE SCRIPTS (Next.js Version)
// =========================================================================
// Copy and paste these into your browser console to test the backend.
// =========================================================================

const API_BASE = "https://u9zfrut1t9.execute-api.ap-south-1.amazonaws.com/dev";

// 1. GET AUTH CONFIG & STATUS 
// Checks if Test OTP is enabled and retrieves Cognito Pool settings
fetch(API_BASE + '/api/dev_only', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ actionItem: 'GetAuthConfig' })
}).then(res => res.json()).then(data => {
  console.log("Auth Config:", data);
});


// 2. WIPE ALL COGNITO & RDS USERS 
// Warning: Wipes all users from BOTH the Cognito User Pool and RDS (DEV/QA only)
fetch(API_BASE + '/api/dev_only', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ actionItem: 'WipeAllUsers' })
}).then(res => res.json()).then(data => {
  console.log("Wipe status:", data);
});


// 3. TEST COGNITO LOGIN 
// Verify the end-to-end Cognito integration using username/password
fetch(API_BASE + '/api/dev_only', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ 
    actionItem: 'TestCognitoLogin', 
    username: 'YOUR_TEST_USERNAME', 
    password: 'YOUR_TEST_PASSWORD' 
  })
}).then(res => res.json()).then(data => {
  console.log("Login Test Result:", data);
});


// 4. WIPE ONLY RDS USERS
// Warning: Wipes all users from PostgreSQL ONLY. Leaves Cognito intact.
fetch(API_BASE + '/api/dev_only', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ actionItem: 'WipeDevRDSUsers' })
}).then(res => res.json()).then(data => {
  console.log("RDS Wipe status:", data);
});


// 5. GET ALL RDS USERS
// Retrieve a complete list of all users stored in the PostgreSQL database
fetch(API_BASE + '/api/dev_only', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ actionItem: 'GetDevAllUsers' })
}).then(res => res.json()).then(data => {
  console.log("All RDS Users:");
  if (data.users) console.table(data.users);
});


// 6. GET SELECTIVE RDS USER
// Retrieve data for a specific user from RDS by their username
fetch(API_BASE + '/api/dev_only', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ actionItem: 'GetDevRDSUser', username: 'TARGET_USERNAME' })
}).then(res => res.json()).then(data => {
  console.log("Selective RDS User:", data);
});
